import type { KnowledgeDomainSeed } from '../create-knowledge-map';
import {
  architectureVisual,
  capacityVisual,
  failureVisual,
  sequenceVisual,
  tokens as t,
  tradeoffVisual,
} from './visuals';
import {
  awsBuildersReference,
  card,
  code,
  googleSreReference,
  h,
  list,
  p,
  primerReference,
} from './shared';

export const foundationDomains: readonly KnowledgeDomainSeed[] = [
  {
    title: '主导面试过程',
    short: '需求、估算与收束',
    summary: '先把题目变成可验证的约束，再让架构回应这些约束。',
    groups: [
      {
        title: '建立答题主线',
        level: 'core',
        references: [primerReference('Study guide and interview approach', '#study-guide')],
        points: [
          card({
            title: '一轮系统设计面试怎样完整作答',
            aliases: ['system design interview', '答题模板', '学习路线'],
            coverage: ['interview.study-guide', 'interview.approach'],
            content: [
              p('系统设计面试是一段由候选人主动推进的技术讨论。好的回答会持续做三件事。确认边界，用数量约束方案，再把主要取舍讲给面试官听。架构图只是这些判断的记录。'),
              sequenceVisual(
                '系统设计面试完整流程',
                [t.client('需求'), t.metric('估算'), t.service('架构'), t.store('深挖'), t.result('取舍')],
                [
                  { title: '确认范围', active: ['需求'], note: '选定核心用户流、非功能目标和暂不处理的能力。' },
                  { title: '得到数量级', active: ['需求', '估算'], note: '用峰值 QPS、存储和带宽约束后续设计。' },
                  { title: '画主链路', active: ['估算', '架构'], note: '先画最少组件，再解释每个组件为何存在。' },
                  { title: '深入风险点', active: ['架构', '深挖'], note: '围绕数据、一致性、热点或故障做深入设计。' },
                  { title: '回到目标', active: ['深挖', '取舍'], note: '说明方案守住什么，代价是什么，下一步如何演进。' },
                ],
              ),
              h('时间怎样分'),
              p('四十分钟的常见分配可以是五分钟澄清，五分钟估算，十分钟高层设计，十五分钟深入核心组件，最后五分钟检查故障与取舍。面试官想深入某个方向时，及时调整，不要为了跑完模板打断讨论。'),
              h('面试官在观察什么'),
              list('能否把含糊需求变成明确假设', '能否用量级排除不合适的方案', '能否解释数据流和故障行为', '能否发现旧方案的问题并修正', '能否在复杂度、成本和可靠性之间做选择'),
              h('练习方式'),
              p('短周期准备先练完整流程和典型题。时间充足以后再补薄弱主题。每次练习都保留估算、架构和复盘记录，同一道题隔几天重做，比连续阅读大量答案更容易暴露缺口。'),
            ],
            references: [primerReference('Study guide and interview approach', '#how-to-approach-a-system-design-interview-question')],
          }),
          card({
            title: '需求、约束与假设',
            aliases: ['functional requirements', 'non-functional requirements', 'SLO', 'use cases'],
            coverage: ['interview.requirements'],
            content: [
              p('需求澄清决定后面的架构有没有判断依据。先选一到两条核心用户流，随后确认规模、读写比例、地域、数据保留和质量目标。无法从题目得到的数据可以作假设，但要让面试官听见。'),
              architectureVisual(
                '从需求到设计约束',
                [
                  ['用户流', [t.client('谁在用'), t.service('做什么'), t.result('得到什么')]],
                  ['质量目标', [t.metric('延迟'), t.metric('可用性'), t.metric('一致性'), t.metric('成本')]],
                  ['边界', [t.muted('暂不处理'), t.store('数据范围'), t.service('外部依赖')]],
                ],
                '每个重要组件都应当能追溯到一条需求或约束。',
              ),
              h('先问核心用户流'),
              list('用户如何创建、读取、更新或删除核心数据', '是否需要搜索、实时推送、排序或离线处理', '哪些能力必须在这轮设计中完成', '哪些能力只记录为后续扩展'),
              h('再问质量目标'),
              p('延迟要说明百分位和操作类型。可用性要说明哪些功能必须持续工作。一致性要落到业务不变量，例如余额不能为负、用户名必须唯一。持久性要说明能否丢数据以及可接受的恢复窗口。'),
              h('把假设写在图旁边'),
              p('例如主要用户在单一区域，读写比为一百比一，图片允许最终一致，支付写入需要强一致。假设被面试官修改时，能看出哪些组件必须跟着变，这正是讨论的价值。'),
            ],
            references: [primerReference('Outline use cases, constraints, and assumptions', '#step-1-outline-use-cases-constraints-and-assumptions')],
          }),
          card({
            title: '容量估算与数量级判断',
            aliases: ['back of the envelope', 'DAU', 'QPS', '带宽', '存储估算'],
            coverage: ['interview.back-of-envelope'],
            content: [
              p('估算的目标是区分量级。十台应用服务器和一千台服务器会带来完全不同的分片、缓存和运维设计。先写单位和假设，保留一位有效数字，结果能支持选型即可。'),
              capacityVisual(
                '一亿日活的读请求估算',
                [
                  ['平均 QPS', [t.metric('1 亿 DAU'), t.plain('×'), t.metric('20 次读取'), t.plain('÷ 86400'), t.result('约 2.3 万')]],
                  ['峰值 QPS', [t.metric('2.3 万'), t.plain('×'), t.metric('3 倍峰值'), t.result('约 7 万')]],
                  ['出站带宽', [t.metric('7 万 QPS'), t.plain('×'), t.metric('20 KB'), t.result('约 1.4 GB/s')]],
                ],
                '这个量级已经足以讨论 CDN、缓存、水平扩展和跨区流量成本。',
              ),
              h('常用计算'),
              code('平均 QPS = 日活 × 每用户每日操作数 ÷ 86400\n峰值 QPS = 平均 QPS × 峰值系数\n存储 = 每日新增 × 单条大小 × 保留天数 × 副本与索引系数\n带宽 = 峰值 QPS × 平均响应大小'),
              h('容易漏掉的放大项'),
              list('客户端一次操作可能触发多个内部请求', '数据库副本、日志和索引会放大写入量', '压缩前后大小不同，图片和视频要单独计算', '热点并不服从全局平均值', '容量要给故障切换和增长留余量'),
              h('估算怎样影响设计'),
              p('内存能容纳完整热点集时，可以优先选择简单的集中式缓存。单表多年后仍只有几十 GB 时，没有必要提前分片。单个名人事件能制造百万级瞬时 fan-out 时，平均 QPS 又不足以指导队列和限流设计。'),
            ],
            references: [primerReference('Back-of-the-envelope calculations', '#back-of-the-envelope-calculations')],
          }),
          card({
            title: '高层架构与核心数据流',
            aliases: ['high level design', 'API', 'data model', 'read path', 'write path'],
            coverage: ['interview.high-level-design'],
            content: [
              p('高层设计先画最短的端到端路径。客户端从哪里进入，哪个服务拥有业务规则，数据写到哪里，读取是否经过缓存，异步工作怎样被触发。每加一个组件，都要说清它解决的约束。'),
              architectureVisual(
                '通用在线服务的最小架构',
                [
                  ['入口', [t.client('客户端'), t.service('负载均衡'), t.service('API 服务')]],
                  ['读取', [t.cache('缓存'), t.store('主数据库'), t.store('读副本')]],
                  ['异步', [t.queue('事件队列'), t.service('Worker'), t.store('对象存储')]],
                ],
                '面试开始阶段保留一条主写链路和一条主读链路，细节随后展开。',
              ),
              h('先定 API 和数据模型'),
              p('API 暴露用户动作，数据模型保存业务不变量。先写关键字段、唯一键、查询条件和排序方式。模型清楚以后，索引、分片键、缓存键和幂等键通常都有了落点。'),
              h('分别走一遍读写'),
              list('写入从鉴权开始，经过校验、持久化、缓存处理和事件发布', '读取从路由开始，经过缓存、数据库和结果组装', '后台任务要说明触发方式、重试、去重和完成状态'),
              h('避免一开始堆满组件'),
              p('搜索引擎、队列、缓存和微服务都有持续成本。当前规模用单库和模块化单体就能满足时，先画简单方案。扩展阶段再根据瓶颈增加组件，面试官才能看见演进依据。'),
            ],
            references: [primerReference('Create a high level design', '#step-2-create-a-high-level-design')],
          }),
          card({
            title: '深入核心组件、扩展与取舍',
            aliases: ['deep dive', 'bottleneck', 'tradeoff', 'scale design'],
            coverage: ['interview.core-components', 'interview.scale-design'],
            content: [
              p('深入设计要选风险最高的部分。订单题通常深挖库存并发和支付幂等，Feed 题通常深挖 fan-out 和热点用户，爬虫题通常深挖 URL 调度、去重和礼貌抓取。按组件平均讲解会浪费时间。'),
              failureVisual(
                '从简单架构演进到可恢复架构',
                [t.client('请求'), t.service('单实例'), t.store('单库')],
                [t.client('请求'), t.danger('实例故障'), t.danger('请求失败')],
                [t.client('请求'), t.service('负载均衡'), t.service('多实例'), t.store('主库与副本')],
                ['简单方案先证明业务链路。', '明确单点故障和恢复时间。', '按目标增加冗余、检测和切换，不跳过演进过程。'],
              ),
              h('选择深入点'),
              list('最容易超过容量上限的组件', '最容易破坏业务不变量的并发路径', '最影响 P99 延迟的依赖', '故障后最难恢复的数据', '成本增长最快的资源'),
              h('每个选择都回答三件事'),
              p('它解决了哪个约束。它引入了什么新问题。什么指标会触发下一次升级。分片能扩大容量，也会增加跨分片查询和迁移成本。缓存能降低延迟，也会引入陈旧数据和击穿风险。'),
              h('收尾检查'),
              p('回到开头的需求和估算，检查容量、数据正确性、故障行为、安全和成本。主动说出一个尚未解决的问题，再说明如果多十分钟会怎样验证或改进，比笼统声称系统高可用更可信。'),
            ],
            references: [primerReference('Design core components and scale the design', '#step-3-design-core-components')],
          }),
        ],
      },
    ],
  },
  {
    title: '理解系统取舍',
    short: '性能、可用性与一致性',
    summary: '把模糊的高性能和高可用变成能计算、能验证的目标。',
    groups: [
      {
        title: '性能与规模',
        level: 'core',
        references: [primerReference('System design topic index', '#index-of-system-design-topics')],
        points: [
          card({
            title: '性能、可扩展性与扩容方式',
            aliases: ['performance', 'scalability', 'vertical scaling', 'horizontal scaling', 'scale up', 'scale out'],
            coverage: ['foundation.performance-scalability', 'start.vertical-scaling', 'start.horizontal-scaling'],
            content: [
              p('性能问题描述一份工作完成得太慢。可扩展性问题描述负载增长以后，系统无法按增加的资源继续承担工作。单用户查询很慢，先查算法、索引和网络。单用户很快，高并发时才变慢，再看资源饱和、锁竞争和扩容。'),
              tradeoffVisual(
                '纵向扩展与横向扩展',
                [
                  ['纵向', [t.service('更强单机'), t.result('改造少'), t.danger('上限与单点')]],
                  ['横向', [t.service('更多实例'), t.result('容量与容错'), t.danger('分布式复杂度')]],
                ],
                '应用层常先做无状态横向扩展，数据库则在索引和纵向扩展之后再考虑分片。',
              ),
              h('纵向扩展'),
              p('增加 CPU、内存、磁盘或网络，改造少，适合早期和难拆分的有状态负载。它受单机规格、故障域和维护窗口限制，机器越大，单位成本通常越高。'),
              h('横向扩展'),
              p('增加实例并分配流量，可以扩大总吞吐并隔离单机故障。应用需要尽量无状态，数据层还要处理分片、复制、协调和在线迁移。横向扩展不会自动修复低效查询。'),
              h('用指标决定升级'),
              list('单请求 CPU 时间和数据库扫描行数', '峰值并发下的 P99 与错误率', 'CPU、内存、连接池和队列利用率', '增加一倍资源后吞吐是否接近增加一倍'),
            ],
            references: [primerReference('Performance vs scalability', '#performance-vs-scalability')],
          }),
          card({
            title: '延迟、吞吐量与尾延迟',
            aliases: ['latency', 'throughput', 'P95', 'P99', 'tail latency'],
            coverage: ['foundation.latency-throughput'],
            content: [
              p('延迟是一份工作从开始到完成的时间，吞吐量是单位时间完成的工作量。优化吞吐可能增加排队和批处理等待，追求最低单请求延迟也可能牺牲资源利用率。面试中要说明目标操作和可接受的百分位。'),
              capacityVisual(
                '一次请求的延迟预算',
                [
                  ['入口', [t.metric('网络 20 ms'), t.service('网关 5 ms')]],
                  ['服务', [t.metric('业务 15 ms'), t.cache('缓存 5 ms')]],
                  ['数据', [t.store('数据库 40 ms'), t.result('总计约 85 ms')]],
                ],
                '总超时必须高于正常预算，又要给重试和上游响应留下空间。',
              ),
              h('为什么要看分位数'),
              p('平均值会掩盖少量很慢的请求。一个页面并行请求二十个服务时，用户体验常由最慢的那个决定。P50 说明常态，P95 和 P99 用来观察尾部，错误率要和延迟一起看。'),
              h('吞吐受什么限制'),
              list('CPU 密集任务受计算资源限制', '数据库写入受锁、日志和磁盘限制', '大对象传输受网络带宽限制', '异步消费者受下游处理速度限制'),
              h('常见优化顺序'),
              p('先减少无效工作和往返，再处理索引、批量、缓存与并发。压测要保持错误率和结果正确，单纯把请求更快地拒绝会提高表面吞吐，却没有增加有效工作量。'),
            ],
            references: [primerReference('Latency vs throughput', '#latency-vs-throughput')],
          }),
        ],
      },
      {
        title: '一致性与可用性',
        level: 'advanced',
        references: [primerReference('Availability vs consistency', '#availability-vs-consistency')],
        points: [
          card({
            title: '可用性、一致性、CAP 与 PACELC',
            aliases: ['CAP', 'PACELC', 'CP', 'AP', 'partition tolerance', 'linearizability'],
            coverage: ['foundation.availability-consistency', 'foundation.cap', 'foundation.cp', 'foundation.ap'],
            content: [
              p('CAP 讨论网络分区发生时，分布式读写系统无法同时保证线性一致和每个未故障节点都响应请求。系统仍要处理分区，只能针对具体操作决定等待或拒绝一部分请求，还是继续响应并接受短暂分歧。'),
              failureVisual(
                '网络分区期间的 CP 与 AP 选择',
                [t.client('客户端'), t.service('节点 A'), t.service('节点 B'), t.result('副本一致')],
                [t.client('写请求'), t.service('节点 A'), t.danger('网络分区'), t.service('节点 B')],
                [t.plain('CP 拒绝不确定写'), t.plain('或'), t.plain('AP 接受写并在恢复后合并')],
                ['节点能够交换最新状态。', 'A 与 B 无法确认对方是否仍在服务。', '选择必须对应余额、库存、点赞等具体业务语义。'],
              ),
              h('CP 与 AP 怎样落到业务'),
              p('唯一库存扣减通常宁可让无法确认多数派的节点拒绝写入。点赞计数可以继续接收，恢复后按事件或版本合并。同一个系统的订单、推荐和在线状态可以采用不同策略。'),
              h('PACELC 补充正常时期'),
              p('没有分区时，跨地域同步复制仍要在一致性和延迟之间取舍。同步等待远端确认能提供更强的顺序保证，写延迟也会上升。异步复制响应更快，读副本可能落后。'),
              h('两个一致性不要混淆'),
              p('CAP 的一致性通常指线性一致。数据库 ACID 中的一致性指事务前后业务约束保持成立。回答时先说业务不变量和操作语义，再讨论读写在分区期间是否能够等待、拒绝或返回旧值。'),
            ],
            references: [
              { title: 'Gilbert and Lynch · Brewer’s conjecture', location: 'CAP 的形式化定义与证明', href: 'https://www.comp.nus.edu.sg/~gilbert/pubs/BrewersConjecture-SigAct.pdf' },
              { title: 'Daniel Abadi · PACELC', location: '正常时期的延迟与一致性取舍', href: 'https://www.cs.umd.edu/~abadi/papers/abadi-pacelc.pdf' },
            ],
          }),
          card({
            title: '弱一致、最终一致与强一致',
            aliases: ['weak consistency', 'eventual consistency', 'strong consistency', 'read your writes'],
            coverage: ['consistency.weak', 'consistency.eventual', 'consistency.strong'],
            content: [
              p('一致性模型约束客户端能观察到哪些读写顺序。它比强、弱两个标签更具体。面试回答要说明写后读、单调读、因果顺序和冲突处理中的哪一项由系统保证。'),
              tradeoffVisual(
                '三类一致性体验',
                [
                  ['弱一致', [t.service('尽力传播'), t.danger('可能长期看不到')]],
                  ['最终一致', [t.queue('异步复制'), t.result('停止写入后收敛')]],
                  ['强一致', [t.store('同步确认'), t.result('已完成写立即可见')]],
                ],
                '语音和直播能容忍丢失片段，Feed 能容忍短暂陈旧，余额与唯一约束通常需要更强保证。',
              ),
              h('弱一致'),
              p('系统尽力传播更新，不承诺某次读取何时看见它。实时语音、视频和游戏状态常选择丢弃过期数据，因为迟到的数据已经没有价值。'),
              h('最终一致'),
              p('没有新写入以后，副本最终收敛。DNS、动态流和异步索引常见这种模式。工程上仍要定义收敛时间、冲突合并、读己之写和删除传播。'),
              h('强一致'),
              p('已完成的写入会被后续读取观察到，代价通常是协调、延迟和分区期间拒绝部分操作。强一致也不能替业务定义正确事务，库存检查和扣减仍要放进同一原子边界。'),
            ],
            references: [primerReference('Consistency patterns', '#consistency-patterns')],
          }),
          card({
            title: '故障切换、复制与可用性数字',
            aliases: ['failover', 'active passive', 'active active', 'availability in numbers', 'nine nines'],
            coverage: ['availability.failover', 'availability.active-passive', 'availability.active-active', 'availability.replication', 'availability.numbers'],
            content: [
              p('可用性要用操作和时间窗口定义。四个九意味着一年大约允许五十二分钟不可用，但一次核心写入失败和一个非核心报表失败的影响不同。SLO 应按用户能否完成关键动作来测量。'),
              tradeoffVisual(
                '主备与双活',
                [
                  ['主备', [t.service('主节点承载'), t.muted('备节点待命'), t.result('冲突少'), t.danger('切换时间')]],
                  ['双活', [t.service('两侧承载'), t.result('容量利用高'), t.danger('冲突与路由复杂')]],
                ],
                '副本必须跨真实故障域，放在同一电源或同一可用区不能抵御区域故障。',
              ),
              h('串联和并联'),
              p('请求必须依次经过多个组件时，总可用性近似为各组件可用性的乘积。两个各为 99.9% 的串联组件，总可用性约为 99.8%。能够独立接管的并联副本会降低同时故障概率，但切换逻辑本身也可能失败。'),
              h('故障切换要回答'),
              list('怎样检测故障并避免瞬时抖动误判', '谁有权成为新主并阻止旧主继续写', '未复制的数据是否允许丢失', '连接和流量怎样迁移', '恢复以后怎样重新加入副本'),
              h('可恢复才算冗余'),
              p('复制能处理硬件故障，也会复制误删除和错误写入。备份、日志重放和恢复演练解决另一类问题。RPO 约束能丢多少数据，RTO 约束多久恢复。两者要经过演练验证。'),
            ],
            references: [primerReference('Availability patterns', '#availability-patterns'), googleSreReference],
          }),
        ],
      },
    ],
  },
];
