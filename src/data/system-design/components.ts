import type { KnowledgeDomainSeed } from '../create-knowledge-map';
import {
  architectureVisual,
  capacityVisual,
  failureVisual,
  sequenceVisual,
  tokens as t,
  tradeoffVisual,
} from './visuals';
import {
  awsBuildersReference,
  azureArchitectureReference,
  card,
  code,
  googleSreReference,
  h,
  list,
  p,
  primerReference,
} from './shared';

export const componentDomains: readonly KnowledgeDomainSeed[] = [
  {
    title: '设计请求路径',
    short: '边缘、服务与安全',
    summary: '沿一次请求经过的路径，理解每一层负责什么。',
    groups: [
      {
        title: '边缘入口',
        level: 'core',
        references: [primerReference('DNS, CDN, load balancers and reverse proxies', '#domain-name-system')],
        points: [
          card({
            title: 'DNS 解析、缓存与流量切换',
            aliases: ['DNS', 'recursive resolver', 'authoritative server', 'TTL', 'GeoDNS'],
            coverage: ['edge.dns'],
            content: [
              p('DNS 把域名解析为可访问的服务入口。客户端通常把递归查询交给本地解析器，解析器再访问根、顶级域和权威服务器。每一层都可能缓存结果，TTL 决定缓存多久。'),
              sequenceVisual(
                'DNS 解析链路',
                [t.client('浏览器'), t.cache('递归解析器'), t.service('根与顶级域'), t.service('权威 DNS'), t.result('服务 IP')],
                [
                  { title: '检查本地缓存', active: ['浏览器', '递归解析器'], note: '命中缓存就直接返回，未命中才继续查询。' },
                  { title: '找到权威服务器', active: ['递归解析器', '根与顶级域', '权威 DNS'], note: '解析器沿委派关系找到负责该域名的权威服务器。' },
                  { title: '返回并缓存', active: ['权威 DNS', '服务 IP'], note: '结果按 TTL 缓存在解析器和客户端。' },
                ],
              ),
              h('TTL 是切流速度与查询量的取舍'),
              p('较长 TTL 能减少权威 DNS 压力，也会让旧地址保留更久。较短 TTL 提高变更灵活性，查询量和成本随之上升。即使 TTL 很短，客户端和运营商也可能有额外缓存。'),
              h('DNS 能做什么'),
              list('按地域或延迟返回不同入口', '把流量分散到多个负载均衡地址', '在计划迁移前提前降低 TTL', '通过健康状态停止返回故障入口'),
              h('DNS 做不了什么'),
              p('它很难提供毫秒级故障切换，也看不到单次请求状态。实时摘除实例应交给负载均衡，连接中的重试交给客户端或网关。多级切流要避免 DNS、负载均衡和应用重试互相放大故障。'),
            ],
            references: [primerReference('Domain name system', '#domain-name-system'), { title: 'RFC 1034', location: 'DNS 的概念、委派与缓存', href: 'https://www.rfc-editor.org/rfc/rfc1034' }],
          }),
          card({
            title: 'CDN、Push 与 Pull',
            aliases: ['CDN', 'push CDN', 'pull CDN', 'edge cache', '回源'],
            coverage: ['edge.cdn', 'edge.push-cdn', 'edge.pull-cdn'],
            content: [
              p('CDN 把可缓存内容放到离用户更近的边缘节点，减少跨地域延迟和源站流量。它适合图片、视频、脚本、下载文件和可安全共享的响应。含用户权限或高频变化的数据要谨慎设置缓存键和有效期。'),
              architectureVisual(
                'CDN 请求与回源',
                [
                  ['访问', [t.client('用户'), t.cache('边缘节点'), t.result('缓存命中')]],
                  ['回源', [t.cache('边缘未命中'), t.service('源站'), t.store('对象存储')]],
                  ['更新', [t.service('发布系统'), t.queue('预热或刷新'), t.cache('边缘节点')]],
                ],
                '缓存命中直接返回。未命中时边缘回源、保存对象，再服务后续请求。',
              ),
              h('Push CDN'),
              p('发布时主动把内容分发到边缘，适合文件数量可控、访问确定、更新不频繁的场景。发布链路更复杂，低热度内容也会占用边缘空间。'),
              h('Pull CDN'),
              p('第一次访问时回源并缓存，适合内容很多、热点难预测的场景。冷内容首请求较慢，热点突发时还要用请求合并和源站保护避免回源风暴。'),
              h('缓存正确性'),
              list('缓存键包含会改变响应的路径、参数和请求头', '私有响应不进入共享缓存', '静态资源使用内容哈希 URL', '刷新操作要考虑全球传播延迟', '源站鉴权防止绕过 CDN 直接访问'),
            ],
            references: [primerReference('Content delivery network', '#content-delivery-network')],
          }),
          card({
            title: '负载均衡、L4/L7 与反向代理',
            aliases: ['load balancer', 'reverse proxy', 'L4', 'L7', 'active active', 'active passive'],
            coverage: ['edge.load-balancer', 'edge.active-passive', 'edge.active-active', 'edge.layer-4', 'edge.layer-7', 'edge.horizontal-scaling', 'edge.reverse-proxy', 'edge.lb-vs-reverse-proxy'],
            content: [
              p('负载均衡把流量分给多个后端，反向代理代表后端接收客户端请求。两者能力经常由同一个产品提供。讨论时应按职责区分，前者关心实例选择和健康，后者还可能处理 TLS、缓存、压缩和请求改写。'),
              architectureVisual(
                '四层与七层路由',
                [
                  ['L4', [t.client('TCP 连接'), t.service('IP 与端口'), t.service('后端实例')]],
                  ['L7', [t.client('HTTP 请求'), t.service('域名与路径'), t.service('业务服务')]],
                  ['部署', [t.service('LB A'), t.service('LB B'), t.result('主备或双活')]],
                ],
                '四层处理连接，七层理解应用协议。横向扩展依赖后端可被任意实例处理。',
              ),
              h('实例怎样被选择'),
              p('轮询适合请求成本相近的服务。最少连接适合长连接或请求时长差异大的服务。加权策略用于不同规格实例和渐进发布。一致性哈希能减少节点变化时的重映射，但会增加热点处理难度。'),
              h('健康检查'),
              list('存活检查判断进程是否需要重启', '就绪检查判断实例是否可以接流量', '深层检查观察关键依赖，但不能因一个次要依赖让全部实例同时摘除', '摘除和恢复要有阈值与抖动保护'),
              h('主备与双活'),
              p('主备部署的状态简单，切换会有恢复时间。双活让两侧同时承载流量，容量利用率更高，配置、会话和故障域必须保持一致。负载均衡自身也要跨故障域部署。'),
            ],
            references: [primerReference('Load balancer and reverse proxy', '#load-balancer')],
          }),
        ],
      },
      {
        title: '应用边界与安全',
        level: 'scenario',
        references: [primerReference('Application layer and security', '#application-layer')],
        points: [
          card({
            title: '应用层、微服务与服务发现',
            aliases: ['application layer', 'microservices', 'service discovery', 'modular monolith'],
            coverage: ['application.layer', 'application.microservices', 'application.service-discovery'],
            content: [
              p('应用层实现业务规则并组织数据访问。早期系统可以先用模块化单体，让模块边界和数据所有权清楚。独立扩展、团队自治、发布频率和故障隔离出现真实需要以后，再拆成服务。'),
              architectureVisual(
                '服务注册与发现',
                [
                  ['注册', [t.service('服务实例'), t.service('注册中心'), t.metric('健康租约')]],
                  ['调用', [t.client('调用方'), t.service('发现或路由层'), t.service('目标实例')]],
                  ['治理', [t.service('配置中心'), t.service('网关'), t.metric('监控')]],
                ],
                '客户端发现把选择逻辑放进调用方，服务端发现把它集中在路由层。',
              ),
              h('服务拆分依据'),
              list('一块业务有清晰的数据所有权', '它需要独立扩容或独立发布', '团队能独立维护契约和运行质量', '故障需要和其他业务隔离'),
              h('拆分带来的成本'),
              p('本地函数调用变成远程调用以后，要处理超时、重试、版本兼容、链路追踪和部分失败。跨服务事务也会变成事件、补偿或工作流。仅按代码行数拆分不会得到这些收益。'),
              h('发现与配置'),
              p('实例启动后注册地址并续租，调用方或路由层选择健康实例。注册表短暂不可用时，客户端应能使用缓存信息。动态配置要版本化、可审计、可回滚，密钥使用专门的秘密管理系统。'),
            ],
            references: [primerReference('Application layer, microservices and service discovery', '#application-layer'), azureArchitectureReference],
          }),
          card({
            title: '身份、权限、数据与滥用防护',
            aliases: ['security', 'authentication', 'authorization', 'encryption', 'rate limit', 'abuse prevention'],
            coverage: ['security.overview'],
            content: [
              p('安全要沿数据流设计。入口验证身份，业务层检查资源权限，服务之间使用可轮换凭据，存储层保护敏感数据，日志避免泄露秘密。最后还要处理恶意流量、枚举、重放和资源耗尽。'),
              architectureVisual(
                '请求经过的安全边界',
                [
                  ['入口', [t.client('客户端'), t.service('WAF 与限流'), t.service('身份认证')]],
                  ['业务', [t.service('权限检查'), t.service('输入校验'), t.service('审计')]],
                  ['数据', [t.store('加密存储'), t.service('密钥管理'), t.metric('访问告警')]],
                ],
                '身份认证回答是谁，权限检查回答他能对哪项资源做什么。',
              ),
              h('威胁从资产和边界开始'),
              p('先列敏感数据、关键操作、外部入口和信任边界。支付、密码重置、管理操作和批量导出需要更强的认证、审批、速率限制和审计。'),
              h('常见控制'),
              list('传输使用 TLS，敏感字段静态加密', '密码使用专用慢哈希，不自行设计加密算法', '服务账号采用最小权限并定期轮换', '写接口使用幂等键和防重放信息', '上传内容限制类型、大小和处理资源', '日志脱敏并控制查询权限'),
              h('设计题中的安全收尾'),
              p('说明一个最可能的滥用方式和对应控制。短链接要处理恶意 URL，爬虫要防止内网探测，聊天要处理垃圾消息和封禁，文件服务要处理越权下载。安全措施也会增加延迟和运维成本。'),
            ],
            references: [{ title: 'OWASP Application Security Verification Standard', location: '身份、会话、访问控制与数据保护检查', href: 'https://owasp.org/www-project-application-security-verification-standard/' }, primerReference('Security', '#security')],
          }),
        ],
      },
    ],
  },
  {
    title: '选择数据系统',
    short: 'SQL、NoSQL 与数据布局',
    summary: '从不变量和访问模式出发，再决定复制、分片与索引。',
    groups: [
      {
        title: '关系数据与扩展',
        level: 'core',
        references: [primerReference('Database and RDBMS', '#database')],
        points: [
          card({
            title: '关系数据库、事务与索引',
            aliases: ['RDBMS', 'SQL', 'ACID', 'transaction', 'index'],
            coverage: ['database.overview', 'database.rdbms'],
            content: [
              p('关系数据库适合结构明确、关联查询多、事务不变量重要的业务。表结构、主键、唯一约束、外键和检查约束把一部分正确性放进数据库。事务再把多个读写组织成原子操作。'),
              architectureVisual(
                '关系数据库读写路径',
                [
                  ['写入', [t.client('业务请求'), t.service('事务'), t.store('WAL'), t.store('数据页')]],
                  ['读取', [t.client('查询'), t.store('索引'), t.store('数据页'), t.result('结果集')]],
                  ['约束', [t.store('唯一键'), t.store('外键'), t.store('检查约束')]],
                ],
                '索引减少扫描，事务日志保证已提交修改能够在故障后恢复。',
              ),
              h('模型先保存不变量'),
              p('用户名唯一、订单状态合法、库存不能随意变负，这些规则优先用约束和事务保护。应用层校验改善错误提示，不能替代并发情况下的数据库约束。'),
              h('索引回应查询模式'),
              list('等值过滤字段通常放在复合索引前部', '范围和排序要考虑索引顺序', '覆盖索引能减少回表', '每个索引都会增加写放大和存储', '低选择性字段单独建索引收益有限'),
              h('事务隔离'),
              p('隔离级别决定并发事务能观察到什么。锁、乐观版本和可串行化事务各有成本。面试时用具体异常说明选择，例如两名用户同时抢最后一件库存时，检查和扣减必须处于同一并发控制边界。'),
            ],
            references: [primerReference('Relational database management system', '#relational-database-management-system-rdbms'), { title: 'PostgreSQL Documentation', location: '事务、索引与并发控制', href: 'https://www.postgresql.org/docs/current/index.html' }],
          }),
          card({
            title: '主从、主主与读副本一致性',
            aliases: ['primary replica', 'master slave', 'master master', 'replication lag', 'read replica'],
            coverage: ['database.primary-replica', 'database.multi-primary'],
            content: [
              p('单主复制把写顺序集中到 primary，replica 承担读取和故障恢复。多主允许多个站点写入，必须定义同一条数据被并发修改时怎样检测、排序和合并。旧资料中的 master/slave 与 master/master 对应这两类模式。'),
              failureVisual(
                '异步复制与故障切换',
                [t.client('写入'), t.store('Primary'), t.queue('复制日志'), t.store('Replica')],
                [t.client('写入已确认'), t.danger('Primary 故障'), t.danger('日志未复制')],
                [t.client('新请求'), t.store('提升 Replica'), t.result('恢复服务')],
                ['写入先在主节点提交，再异步发送。', '已确认但尚未复制的数据可能丢失。', '选主前要确认复制位点，并阻止旧主重新写入。'],
              ),
              h('同步与异步'),
              p('同步等待副本确认，RPO 更小，写延迟受副本和网络影响。异步先在主节点确认，吞吐和延迟更好，故障时可能丢失尚未复制的数据。半同步可以在两者之间取舍。'),
              h('读己之写'),
              p('用户刚修改资料就从落后副本读取，会看到旧值。可以在短时间内读主库，记录复制位点后等待副本追上，或对同一会话做粘性路由。不同方案影响主库负载和复杂度。'),
              h('防止脑裂'),
              list('选主使用多数派和递增任期', '共享资源写入带 fencing token', '旧主恢复后先降级并追赶日志', '故障切换流程经过定期演练'),
            ],
            references: [primerReference('Database replication', '#replication')],
          }),
          card({
            title: '数据库联合、分片与迁移',
            aliases: ['federation', 'sharding', 'partition key', 'consistent hashing', 'resharding'],
            coverage: ['database.federation', 'database.sharding'],
            content: [
              p('联合按业务边界把表拆到不同数据库，分片按键把同一类数据分布到多个节点。联合适合清晰的业务所有权，分片解决单表和单库容量。两者都会让跨边界事务和查询更难。'),
              architectureVisual(
                '分片路由与热点',
                [
                  ['路由', [t.client('请求带用户 ID'), t.service('分片路由'), t.store('分片 1 或 2')]],
                  ['广播', [t.client('缺少分片键'), t.service('Scatter'), t.store('全部分片')]],
                  ['热点', [t.client('超级租户'), t.danger('单分片过载'), t.service('二级拆分')]],
                ],
                '好的分片键让高频请求定向到一个分片，同时避免少数值占据大部分流量。',
              ),
              h('分片键检查'),
              list('高频查询是否携带完整分片键', '基数是否足够高', '数据和流量是否均匀', '单个租户能否超过一个分片容量', '扩容和迁移时能否双写与校验'),
              h('范围与哈希'),
              p('范围分片支持范围查询，时间和地域键容易形成热点。哈希分片分布更均匀，会打散范围查询。一致性哈希和虚拟节点减少节点变化时的移动量，仍要处理大租户和跨分片聚合。'),
              h('在线迁移'),
              p('先建立新路由和目标分片，后台复制历史数据，增量变更通过日志或双写追平，校验数量与校验和，随后切读、切写并保留回滚窗口。双写失败和顺序问题必须有补偿。'),
            ],
            references: [primerReference('Federation and sharding', '#federation')],
          }),
          card({
            title: '反范式、查询计划与 SQL 调优',
            aliases: ['denormalization', 'SQL tuning', 'query plan', 'N+1', 'covering index'],
            coverage: ['database.denormalization', 'database.sql-tuning'],
            content: [
              p('反范式用重复数据减少读取时的关联与计算，代价是写入和修复更复杂。SQL 调优先看真实查询计划、扫描行数和等待事件，再决定索引、改写、预计算或数据模型调整。'),
              tradeoffVisual(
                '范式化与反范式',
                [
                  ['范式化', [t.store('单一事实来源'), t.result('更新简单'), t.danger('读取关联')]],
                  ['反范式', [t.store('预计算副本'), t.result('读取快'), t.danger('写放大与陈旧')]],
                ],
                '读多写少、查询稳定的视图适合预计算。强一致更新路径要谨慎复制字段。',
              ),
              h('先测再改'),
              list('记录慢查询和高频查询', '使用 EXPLAIN 查看访问路径', '检查估算行数和实际行数差异', '确认时间花在 CPU、磁盘、锁还是网络', '用生产相近的数据量复测'),
              h('常见手段'),
              p('合适的复合索引、批量读取、消除 N+1、减少返回列和避免无界分页往往先见效。分区裁剪、物化视图和反范式适合稳定的大查询。升级机器和增加副本无法修复每次扫描整表的 SQL。'),
              h('维护正确性'),
              p('重复字段要指定唯一来源和同步方式。事务内同步更新保证强一致，事件异步更新允许短暂陈旧。重建任务、校验脚本和版本字段帮助修复历史差异。'),
            ],
            references: [primerReference('Denormalization and SQL tuning', '#denormalization'), { title: 'PostgreSQL · Using EXPLAIN', location: '读取查询计划与执行统计', href: 'https://www.postgresql.org/docs/current/using-explain.html' }],
          }),
        ],
      },
      {
        title: 'NoSQL 与选型',
        level: 'scenario',
        references: [primerReference('NoSQL and SQL or NoSQL', '#nosql')],
        points: [
          card({
            title: '键值、文档、宽列与图数据库',
            aliases: ['NoSQL', 'key value', 'document store', 'wide column', 'graph database'],
            coverage: ['database.nosql', 'database.key-value', 'database.document', 'database.wide-column', 'database.graph'],
            content: [
              p('NoSQL 是多类数据系统的集合。选型要看访问模式、事务边界、扩展方式和运维能力。把所有非关系数据库放进同一类，会掩盖它们完全不同的查询与一致性模型。'),
              tradeoffVisual(
                '四类 NoSQL 的主要访问方式',
                [
                  ['键值', [t.store('key → value'), t.result('点查与缓存')]],
                  ['文档', [t.store('JSON 文档'), t.result('聚合对象')]],
                  ['宽列', [t.store('分区键与排序键'), t.result('大规模有序写')]],
                  ['图', [t.store('点与边'), t.result('多跳关系')]],
                ],
                '同一产品可能跨越多个类别，仍要以实际查询、索引和事务能力为准。',
              ),
              h('键值和文档'),
              p('键值数据库通过键直接定位值，适合会话、缓存和简单状态。文档数据库保存聚合对象并支持字段索引，适合结构会演进、常按完整对象读写的业务。跨文档事务和复杂关联要确认产品能力。'),
              h('宽列和图'),
              p('宽列系统围绕分区键和排序键组织大规模数据，查询必须顺着模型设计。图数据库把多跳关系当作核心操作，适合路径、推荐和关系分析，超大规模在线图仍要注意分区边界。'),
              h('先问的四个问题'),
              list('最高频查询是否能由主键或索引直接完成', '是否需要跨记录事务和唯一约束', '单分区最大数据与流量是多少', '团队能否运行备份、迁移和故障恢复'),
            ],
            references: [primerReference('NoSQL', '#nosql')],
          }),
          card({
            title: 'SQL 与 NoSQL 怎样选择',
            aliases: ['SQL vs NoSQL', 'polyglot persistence', 'database selection'],
            coverage: ['database.sql-vs-nosql'],
            content: [
              p('数据库选型从不变量和访问模式开始。关系模型能直接表达事务、约束和临时查询时，SQL 通常是稳妥起点。访问模式固定、数据量巨大、分区边界清楚时，专用 NoSQL 系统可能更合适。'),
              tradeoffVisual(
                '数据库选型信号',
                [
                  ['SQL', [t.result('事务与约束'), t.result('关联与临时查询'), t.danger('水平分片成本')]],
                  ['NoSQL', [t.result('固定访问模式'), t.result('原生水平扩展'), t.danger('约束与查询受限')]],
                ],
                '流行度和理论标签不能替代实际查询、容量、故障与团队经验。',
              ),
              h('优先 SQL 的信号'),
              list('多个实体要在一个事务中更新', '唯一约束和参照完整性重要', '查询方式会持续变化', '数据量仍在单库或成熟分区能力范围内'),
              h('考虑 NoSQL 的信号'),
              list('请求能够稳定携带分区键', '写入规模超过单关系库边界', '数据天然是文档、图或时序结构', '业务能接受产品提供的一致性和查询限制'),
              h('混合使用的代价'),
              p('同一份事实进入多个数据库后，要指定主数据源、同步方式、延迟目标和重建流程。每增加一种存储，都增加备份、监控、升级和排障成本。先证明单一系统不能满足目标。'),
            ],
            references: [primerReference('SQL or NoSQL', '#sql-or-nosql')],
          }),
        ],
      },
    ],
  },
  {
    title: '控制延迟与流量',
    short: '缓存、异步与通信',
    summary: '让快路径更快，也让慢路径有队列、背压和失败语义。',
    groups: [
      {
        title: '缓存',
        level: 'core',
        references: [primerReference('Cache', '#cache')],
        points: [
          card({
            title: '客户端、CDN、Web、数据库与应用缓存',
            aliases: ['cache', 'client cache', 'CDN cache', 'web server cache', 'database cache', 'query cache', 'object cache'],
            coverage: ['cache.overview', 'cache.client', 'cache.cdn', 'cache.web-server', 'cache.database', 'cache.application', 'cache.query', 'cache.object'],
            content: [
              p('缓存用更快、更近的副本减少延迟和下游负载。缓存放得越靠近用户，命中收益越大，失效和权限控制也越分散。选择位置以前，先确定缓存对象、键、有效期和允许陈旧多久。'),
              architectureVisual(
                '多层缓存路径',
                [
                  ['边缘', [t.client('浏览器缓存'), t.cache('CDN'), t.cache('反向代理')]],
                  ['应用', [t.service('API'), t.cache('对象缓存'), t.cache('查询缓存')]],
                  ['数据', [t.store('数据库缓冲池'), t.store('持久化数据')]],
                ],
                '上层命中会跳过后续层。任一层的键和失效规则错误，都可能返回错误用户或旧版本数据。',
              ),
              h('各层解决的问题'),
              p('客户端和 CDN 适合可公开复用的静态资源。Web 服务器缓存完整响应。应用缓存保存聚合对象和昂贵计算。查询缓存绑定参数和结果集。数据库缓冲池自动缓存数据页与索引页。'),
              h('缓存键'),
              list('包含资源身份和版本', '包含会改变结果的租户、语言和权限范围', '避免把高基数无复用请求塞入缓存', '大对象和小对象分开管理', '记录命中率、加载延迟和淘汰率'),
              h('缓存失效风险'),
              p('穿透表示查询不存在的键持续落到数据库。击穿表示热点键失效后大量请求同时回源。雪崩表示大批键同时过期。负缓存、请求合并、过期抖动和分级限流分别处理这些问题。'),
            ],
            references: [primerReference('Cache locations', '#cache')],
          }),
          card({
            title: 'Cache-aside、Write-through、Write-behind 与 Refresh-ahead',
            aliases: ['cache aside', 'write through', 'write behind', 'write back', 'refresh ahead', '缓存一致性'],
            coverage: ['cache.update', 'cache.cache-aside', 'cache.write-through', 'cache.write-behind', 'cache.refresh-ahead'],
            content: [
              p('缓存更新模式规定读取未命中和写入时谁负责缓存与数据库。模式名称没有统一实现保证，面试中要把读写顺序、失败窗口和允许陈旧时间说清楚。'),
              sequenceVisual(
                'Cache-aside 的一次写后读',
                [t.client('应用'), t.cache('缓存'), t.store('数据库'), t.result('返回结果')],
                [
                  { title: '更新数据库', active: ['应用', '数据库'], note: '先提交权威数据，写入成功以后再处理缓存。' },
                  { title: '删除缓存', active: ['应用', '缓存'], note: '删除旧值，让后续读取重新加载。' },
                  { title: '未命中回源', active: ['缓存', '数据库', '返回结果'], note: '并发回源使用请求合并，避免热点击穿。' },
                ],
              ),
              h('四种模式'),
              list('Cache-aside 由应用读库并填充，写入后删除或更新缓存', 'Write-through 通过缓存同步写入数据库，写延迟包含两层', 'Write-behind 先写缓存再异步落库，吞吐高但要处理缓存故障和重放', 'Refresh-ahead 在过期前刷新热点，适合可预测访问'),
              h('一致性窗口'),
              p('先删缓存再写数据库可能让并发读把旧值重新填回。先写数据库再删缓存的主要风险是删除失败。可以用重试队列、版本号、CDC 失效和短 TTL 缩小窗口。要求线性一致的读取通常不该依赖普通旁路缓存。'),
              h('怎样评估'),
              p('同时观察命中率、回源 QPS、陈旧时间、加载失败和数据库峰值。高命中率不一定有价值，若缓存的是很便宜的查询，维护它的复杂度可能高于节省的资源。'),
            ],
            references: [primerReference('When to update the cache', '#when-to-update-the-cache')],
          }),
        ],
      },
      {
        title: '异步与协议',
        level: 'scenario',
        references: [primerReference('Asynchronism and communication', '#asynchronism')],
        points: [
          card({
            title: '消息队列、任务队列与背压',
            aliases: ['message queue', 'task queue', 'back pressure', 'consumer lag', 'dead letter queue'],
            coverage: ['async.overview', 'async.message-queue', 'async.task-queue', 'async.back-pressure'],
            content: [
              p('异步把接收请求和完成工作分开。消息队列传递业务事件，任务队列分配可执行工作，两者都要定义持久性、投递语义、顺序、重试、去重和积压上限。队列只转移峰值，不能消灭下游容量不足。'),
              architectureVisual(
                '带背压的异步链路',
                [
                  ['生产', [t.client('请求'), t.service('生产者'), t.queue('持久队列')]],
                  ['消费', [t.queue('分区'), t.service('消费者组'), t.store('结果存储')]],
                  ['保护', [t.metric('积压监控'), t.danger('限流或拒绝'), t.queue('死信队列')]],
                ],
                '消费者处理速度持续低于生产速度时，系统必须减慢入口或扩大消费能力。',
              ),
              h('投递与幂等'),
              p('至少一次投递允许重复，需要消费者幂等。至多一次可能丢消息。所谓恰好一次通常只在特定边界内成立，外部副作用仍需幂等键、事务性写入或去重记录。'),
              h('顺序与扩展'),
              p('全局顺序限制并行度。常见做法按订单、用户或会话键分区，只保证同一键内有序。失败消息若一直阻塞分区，要有限重试、隔离和人工修复路径。'),
              h('背压信号'),
              list('队列积压量和最老消息年龄', '消费者处理延迟和失败率', '下游连接池与写入吞吐', '单分区热点', '重试与死信增长速度'),
            ],
            references: [primerReference('Asynchronism', '#asynchronism'), { title: 'Apache Kafka Documentation', location: '消费者组、分区与投递机制', href: 'https://kafka.apache.org/documentation/' }],
          }),
          card({
            title: 'TCP、UDP 与连接容量',
            aliases: ['TCP', 'UDP', 'connection', 'packet loss', 'head of line blocking'],
            coverage: ['communication.overview', 'communication.tcp', 'communication.udp'],
            content: [
              p('TCP 提供可靠、有序的字节流，应用仍要定义消息边界和请求超时。UDP 保留数据报边界，不保证送达、顺序和去重，应用可以按场景决定哪些数据值得重传。协议选择由数据价值和时效决定。'),
              tradeoffVisual(
                'TCP 与 UDP',
                [
                  ['TCP', [t.result('可靠有序'), t.result('拥塞控制'), t.danger('队头阻塞')]],
                  ['UDP', [t.result('报文与低开销'), t.result('可丢弃过期包'), t.danger('可靠性自理')]],
                ],
                '订单和文件传输需要完整可靠，实时语音更在意新数据及时到达。',
              ),
              h('TCP 连接的成本'),
              p('每条长连接占文件描述符、内核缓冲区、应用状态和心跳流量。负载均衡、NAT 和网关也有连接表上限。估算连接数时要把重连峰值和空闲超时纳入。'),
              h('UDP 的应用责任'),
              list('决定是否重传和重传几次', '处理乱序和重复', '控制发送速率避免拥塞', '限制数据报大小避免分片', '设计鉴权与防放大攻击'),
              h('超时和半开连接'),
              p('网络断开不一定立刻被对端感知。应用要有读写超时、心跳和连接回收。大规模客户端同时重连时加入指数退避和随机抖动，避免恢复过程再次压垮入口。'),
            ],
            references: [primerReference('TCP and UDP', '#transmission-control-protocol-tcp'), { title: 'RFC 9293', location: 'TCP 规范', href: 'https://www.rfc-editor.org/rfc/rfc9293' }, { title: 'RFC 768', location: 'UDP 规范', href: 'https://www.rfc-editor.org/rfc/rfc768' }],
          }),
          card({
            title: 'RPC、REST 与远程失败',
            aliases: ['RPC', 'REST', 'HTTP API', 'gRPC', 'idempotent retry'],
            coverage: ['communication.rpc', 'communication.rest'],
            content: [
              p('RPC 以操作和强类型契约组织调用，REST 以资源和 HTTP 语义组织接口。两种方式都经过网络，都会超时、重复、部分完成和版本不一致。接口风格无法消除远程调用的失败性质。'),
              sequenceVisual(
                '一次超时后的安全重试',
                [t.client('调用方'), t.service('API'), t.store('幂等记录'), t.result('原结果')],
                [
                  { title: '首次调用', active: ['调用方', 'API'], note: '服务端可能已经完成操作，但响应在网络中丢失。' },
                  { title: '带同一幂等键重试', active: ['调用方', 'API', '幂等记录'], note: '服务端识别同一业务请求，不重复产生副作用。' },
                  { title: '返回已保存结果', active: ['幂等记录', '原结果'], note: '调用方得到首次执行的状态和响应。' },
                ],
              ),
              h('RPC 的特点'),
              p('接口定义语言和代码生成让内部服务契约清晰，二进制编码通常更紧凑。调用看起来像本地函数时，开发者容易忽略超时和部分失败。流式调用、负载均衡和跨语言支持要看具体框架。'),
              h('REST 的特点'),
              p('HTTP 方法、状态码、缓存和中间代理形成通用生态，适合公开 API 和资源操作。接口要保持资源身份、方法幂等性和错误结构一致，避免把所有动作塞进模糊的 POST。'),
              h('两者都要设计'),
              list('端到端超时预算', '可重试错误和最大次数', '幂等与去重', '版本兼容', '鉴权与限流', '请求追踪和错误归因'),
            ],
            references: [primerReference('RPC and REST', '#remote-procedure-call-rpc'), { title: 'RFC 9110', location: 'HTTP 语义与方法性质', href: 'https://www.rfc-editor.org/rfc/rfc9110' }],
          }),
        ],
      },
    ],
  },
  {
    title: '补齐现代工程能力',
    short: '正确性、韧性与运行',
    summary: 'Primer 之外继续补足今天线上系统绕不开的工程问题。',
    groups: [
      {
        title: '故障与正确性',
        level: 'advanced',
        references: [awsBuildersReference, googleSreReference],
        points: [
          card({
            title: '限流、超时、重试、熔断与负载舍弃',
            aliases: ['rate limit', 'timeout', 'retry', 'circuit breaker', 'load shedding', 'bulkhead'],
            content: [
              p('流量保护要在系统耗尽资源以前行动。限流控制进入量，超时限制等待时间，重试处理短暂失败，熔断减少对持续故障依赖的调用，负载舍弃在过载时保住高价值请求。组合错误会把小故障放大。'),
              failureVisual(
                '重试放大与恢复',
                [t.client('100 请求'), t.service('服务 A'), t.service('服务 B'), t.result('正常')],
                [t.client('100 请求'), t.service('三层各重试 3 次'), t.danger('下游 2700 次尝试')],
                [t.client('限流'), t.service('单层重试预算'), t.danger('快速失败'), t.result('核心流量存活')],
                ['正常请求只调用一次下游。', '多层独立重试会按乘法放大负载。', '在一个边界集中重试，并用退避、抖动和预算限制总量。'],
              ),
              h('超时预算'),
              p('上游总超时要覆盖自身处理和下游调用。每层超时小于剩余预算。连接超时和读取超时分开。阈值过短会制造假失败，过长会占满线程、连接和队列。'),
              h('重试条件'),
              list('操作幂等或带幂等键', '错误是短暂且明确可重试', '使用指数退避和随机抖动', '限制最大次数和总时间', '记录重试带来的额外负载'),
              h('过载时保住什么'),
              p('按用户、租户、接口和优先级分层限流。队列达到上限以后拒绝低价值工作，返回明确的重试建议。舱壁把连接池、线程池和配额隔开，避免一个依赖拖垮整个进程。'),
            ],
            references: [awsBuildersReference, { title: 'Google SRE · Handling Overload', location: '过载、准入控制与负载舍弃', href: 'https://sre.google/sre-book/handling-overload/' }],
          }),
          card({
            title: '幂等、Outbox、Saga 与分布式锁',
            aliases: ['idempotency', 'transactional outbox', 'Saga', 'distributed lock', 'fencing token'],
            content: [
              p('跨服务流程无法依赖一个本地事务完成所有写入。工程上常用幂等键消除重复副作用，用 Outbox 可靠发布事件，用 Saga 组织多步操作和补偿，用租约与 fencing token 限制并发执行者。'),
              sequenceVisual(
                '订单与事件的事务性 Outbox',
                [t.client('下单'), t.service('订单服务'), t.store('订单与 Outbox'), t.queue('消息队列'), t.service('库存服务')],
                [
                  { title: '同一事务写入', active: ['订单服务', '订单与 Outbox'], note: '订单和待发布事件一起提交，避免只成功一半。' },
                  { title: '后台发布', active: ['订单与 Outbox', '消息队列'], note: '发布器可重复读取未完成事件并重试。' },
                  { title: '幂等消费', active: ['消息队列', '库存服务'], note: '消费者按事件 ID 去重，再修改自己的数据。' },
                ],
              ),
              h('Saga'),
              p('Saga 把长事务拆成多个本地事务。每一步成功后推进，失败时执行有业务含义的补偿。退款能补偿扣款，已经发送的邮件只能追加说明。补偿不是数据库回滚，要允许失败和人工介入。'),
              h('分布式锁'),
              p('锁服务故障、租约过期和进程暂停会让旧持有者继续工作。共享资源应检查单调递增的 fencing token，拒绝旧令牌写入。能用唯一约束、版本号或队列串行化解决时，通常比锁更清楚。'),
              h('评测正确性'),
              list('重复请求不会重复扣款或创建资源', '进程在任一步骤崩溃后能够恢复', '消息乱序和重复仍保持不变量', '补偿失败有可观测状态和人工入口'),
            ],
            references: [awsBuildersReference, { title: 'Azure Architecture Center · Transactional Outbox', location: '跨服务事件与本地事务的一致性', href: 'https://learn.microsoft.com/en-us/azure/architecture/databases/guide/transactional-outbox-cosmos' }],
          }),
          card({
            title: 'Quorum、共识、时钟与冲突',
            aliases: ['quorum', 'consensus', 'Raft', 'Paxos', 'logical clock', 'vector clock', 'conflict resolution'],
            content: [
              p('Quorum 用读写副本数量的交集增加读到最新值的机会。共识让多个节点在故障和消息延迟下，对一串决定达成一致。时钟和版本描述事件关系，冲突合并则定义并发更新如何收敛。它们解决的层次不同。'),
              architectureVisual(
                '三副本多数派写入',
                [
                  ['提议', [t.client('客户端'), t.service('Leader'), t.store('副本 A')]],
                  ['复制', [t.store('副本 B'), t.store('副本 C'), t.metric('至少 2 票')]],
                  ['提交', [t.service('Leader'), t.result('多数确认'), t.client('响应')]],
                ],
                '三副本容忍一个节点失联。多数派仍要结合任期、日志匹配和旧主隔离。',
              ),
              h('Quorum 的边界'),
              p('经典条件 N、W、R 中，W 加 R 大于 N 能形成交集，但延迟、副本版本和并发写仍要由协议处理。只套公式不会自动得到线性一致。'),
              h('共识解决什么'),
              p('共识常用于选主、配置和复制日志。业务系统通常依赖数据库、协调服务或消息系统提供的共识能力，无需在面试里从头实现 Paxos。要能说明多数派丢失时为何停止写入。'),
              h('时间与冲突'),
              list('物理时钟会漂移，不能单独证明因果顺序', 'Lamport Clock 给事件建立可比较顺序', 'Vector Clock 能识别并发更新，元数据随参与者增长', '最后写入获胜简单但可能静默丢数据', '集合合并、版本向量和 CRDT 适合可组合状态'),
            ],
            references: [{ title: 'Raft Consensus Algorithm', location: '可理解的共识算法与复制状态机', href: 'https://raft.github.io/' }, azureArchitectureReference],
          }),
          card({
            title: 'SLO、可观测性、RPO 与 RTO',
            aliases: ['SLO', 'SLI', 'SLA', 'error budget', 'observability', 'RPO', 'RTO', 'disaster recovery'],
            content: [
              p('系统上线以后要知道用户是否成功、故障在哪里、多久能恢复。SLI 是测量方式，SLO 是内部目标，SLA 是对外承诺。RPO 约束可接受的数据丢失窗口，RTO 约束服务恢复时间。'),
              architectureVisual(
                '从请求到告警和恢复',
                [
                  ['业务', [t.client('用户请求'), t.service('服务'), t.result('成功或失败')]],
                  ['遥测', [t.metric('指标'), t.metric('日志'), t.metric('Trace')]],
                  ['响应', [t.metric('SLO 告警'), t.service('值班处置'), t.store('备份恢复')]],
                ],
                '遥测围绕用户动作和关键依赖组织，告警要能指导具体处置。',
              ),
              h('四类信号'),
              list('请求速率', '错误率', '延迟分布', '资源饱和度'),
              h('错误预算'),
              p('SLO 允许的失败量形成错误预算。预算消耗过快时减少高风险发布，把精力转向可靠性。只按机器 CPU 告警容易产生噪声，用户成功率和最老队列消息更接近真实影响。'),
              h('恢复目标需要演练'),
              p('同步复制不能替代备份，因为误删除也会同步。备份成功日志不能证明可恢复。定期在隔离环境恢复数据，校验完整性、密钥和依赖，记录实际 RPO 与 RTO。'),
            ],
            references: [googleSreReference, { title: 'AWS Well-Architected · Reliability Pillar', location: '恢复、测试与故障管理', href: 'https://docs.aws.amazon.com/wellarchitected/latest/reliability-pillar/welcome.html' }],
          }),
        ],
      },
    ],
  },
];
